## Place your cursor anywhere on the below line. Then, hit `Ctrl + Enter`
## (or issue `R: Run Selection/Line` command).
source("library.R")

## 1. Place your cursor anywhere on the below line. Then, hit `Ctrl + Enter`
##    (or issue `R: Run Selection/Line` command).
## 2. Hover your mouse over `add` function below.
## 3. Place your cursor on `add` function and hit `F12`.
print(add(1, 2))
